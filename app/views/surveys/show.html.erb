<% provide(:title, @survey.name) %>
<div class="row" xmlns="http://www.w3.org/1999/html">
  <aside class="span4">
    <section>
      <h1>
        <%= @survey.name %>
      </h1>
      <p><%= @survey.created_at.strftime("%m/%d/%Y") %></p>
      <p><%= pluralize(@survey.bookings.count, "booking") %></p>
      <p><%= @survey.csv_file %></p>
      <p><%= link_to "Edit Survey", edit_survey_path(@survey) %></p>
    </section>
  </aside>
</div>